# E2V Event-to-Voxel 3D Reconstruction — Paper Reproduction
# ==========================================================
# Python 3.10+ required, CUDA 11.8 or 12.1 recommended

# Core — install torch with CUDA first:
# pip install torch==2.1.0+cu121 torchvision==0.16.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# OR for CUDA 11.8:
# pip install torch==2.1.0+cu118 torchvision==0.16.0+cu118 --index-url https://download.pytorch.org/whl/cu118

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.22.0
scipy>=1.8.0
matplotlib>=3.5.0
pyyaml>=6.0
tqdm>=4.64.0
tensorboard>=2.10.0
